{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "App notification preferences window",
  "windows": ["settings"],
  "permissions": ["core:default", "store:default", "dialog:default", "notification:default"]
}
