{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "loop",
  "description": "Capability for the floating /loop popup window",
  "windows": ["loop"],
  "permissions": ["core:default"]
}
