{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sessions",
  "description": "Capability for the floating /sessions window.",
  "windows": ["sessions"],
  "permissions": ["core:default"]
}
