---
"polakapi": patch
---

Keep terminals at the latest output during streaming, pane and project switching, and resizing. Require versioned changesets for pull requests and add a Changesets workflow for desktop releases.
